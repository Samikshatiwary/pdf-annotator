import { apiClient } from '../apiclient';

// Placeholder for future collaboration features
export const collaborationAPI = {
  shareWithUser: async (resourceId, userId, permissions) => {
    // Will be implemented when backend adds collaboration endpoints
    throw new Error('Collaboration API not implemented yet');
  },
};